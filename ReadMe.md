# TypeScript Empty Playground

```shell
git clone https://github.com/webia1/typescript-playground.git
cd typescript-playground
npm install
npm i -g yarn
npm i -g ts-node
npm start
```

p.s. package-lock.json -> .gitignore
